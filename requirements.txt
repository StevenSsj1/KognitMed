# Install the local src-layout package so `kognitmed` is importable.
-e .

# Web API and chatbot frontend
fastapi>=0.115.0
uvicorn[standard]>=0.32.0

# Configuration and validation
pydantic>=2.9.0
pydantic-settings>=2.6.0

# LLM providers
openai>=1.54.0
google-generativeai>=0.8.0

# Security
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4

# Logging and HTTP clients
structlog>=24.4.0
httpx>=0.27.0

# Data stores
# MongoDB is intentionally disabled for now.
# motor>=3.6.0
chromadb>=0.6.0
