services:
  redis:
    image: redis:alpine
    container_name: hackiathon_redis
    ports:
      - "6379:6379"
    networks:
      - hackiathon_network

networks:
  hackiathon_network:
    external: true
